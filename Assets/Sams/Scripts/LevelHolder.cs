using UnityEngine;

public class LevelHolder : MonoBehaviour
{
    //Used in the level selector to know what level to spawn and what scene to acsess
    public int index;
    public LevelSystem level;
    public AudioClip clip;
}
